extends ../layouts/LayoutComponent
include ../mixins/components/Modal

prepend styles 

prepend scripts

append variables
  - var pageTitle = 'Modals'

append component 
  +BootstrapComponentLink({
    link: `https://getbootstrap.com/docs/${bootstrapVersion}/components/modal/`,
    name: pageTitle,
  })

  +ModalExampleDemo
  +ModalExampleFadeIn
  +modalSize

 
  